{
  "name": "reachiq",
  "version": "0.1.0",
  "private": true,
  "description": "ReachIQ B2B outreach automation platform",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "test": "python3 -m pytest backend/tests"
  },
  "engines": {
    "node": ">=20"
  }
}
